# SOW tracking is a local planning aid — deliberately not published in this repo
.agents/sow/

# Local app data (the CLI's own SQLite database)
*.db

# Dart/Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
*.g.dart.bak

# Android/iOS build-tool-specific ignores live in android/.gitignore
# (generated by `flutter create`, more precise than anything hand-written here)

# Editor — this project uses VS Code, not Android Studio/IntelliJ
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
.idea/
*.iml

# OS
.DS_Store
